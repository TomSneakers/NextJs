import { lusitana } from '../../ui/fonts';

export default function Page() {
    return (
        <main className="flex min-h-screen flex-col p-6">
        <p className={`${lusitana.className}`}>Factures Page</p>
        </main>
    );
    }